<?php if ( $block->isKlaviyoEnabled() && $block->getPublicApiKey() ): ?>
    <script>
        require([
           'jquery',
           'mage/url'
        ], function (jQuery, url) {
          jQuery.ajax({
              url: url.build('reclaim/checkout/reload'),
              method: 'POST',
              data: {}
          });
        });
    </script>
<?php endif; ?>
